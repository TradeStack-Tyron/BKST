[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd bskt_app/server && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/test-cors"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

[environments.production.variables]
PYTHONPATH = "/app/bskt_app/server"
